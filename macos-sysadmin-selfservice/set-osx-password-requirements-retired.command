#!/bin/bash
sudo pwpolicy -setglobalpolicy "minChars=10 requiresNumeric=1 requiresAlpha=1 r$
mail -s "$(scutil --get LocalHostName) - Password Policy Set" day9consulting@gm$
$(scutil --get LocalHostName) - DayNine password policy was set.
EOF
